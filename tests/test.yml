---

- hosts: localhost
  remote_user: root
  connection: local
  become: true

  roles:
    - src: git+https://github.com/deekayen/ansible-role-etckeeper.git
      name: deekayen.etckeeper
      version: "{{ version }}"
